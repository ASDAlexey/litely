{
  "name": "litely-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Litely landing page",
  "scripts": {
    "build": "node build.js",
    "release": "node build.js && git add docs/ && git status"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "html-minifier-terser": "^7.2.0",
    "sharp": "^0.34.5",
    "terser": "^5.37.0"
  }
}
